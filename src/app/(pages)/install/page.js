import Install from "@/components/ui/Install";

export const metadata = {
  title: "Install",
};
export default function page(){
    return(
        <>
        <Install />
        </>
    )
}